#include <stdio.h>
#define MAXLINE = 1000

//exercise 1-17: Write a program to print all input lines that are longer than a specified number of characters.

// initalize
int getarg();
int getline(char line[], int maxline);
void copyInto(char from[], char to[], int startIndex);

int main()
{
    return 0;
}
